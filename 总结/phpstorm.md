格式化代码
	ctrl+alt+i

搜索全局
	double shift

调到行首
	shift+a




	  /*switch ($order_info['pro_type']) {
            case 1:
                $pro_info = $order_info['pro_type'].'_'.$order_info['product_id'];
                break;
            case 2:
                $pro_info = $order_info['pro_type'].'_'.$order_info['id'];
                break;
            case 3:
                $pro_info = $order_info['pro_type'].'_'.$order_info['integral_pro_id'];
                break;
        }

        $parent_id = $order_info['parent_id'];
        $batch_no = $order_info['batch_no'];*/
       /* unset($order_info['batch_no'], $order_info['parent_id'], $order_info['pro_type'],
            $order_info['product_id'], $order_info['integral_pro_id']);*/
